# sak2（サクサク）

**Windows 用の、軽くて速い 2 画面ファイラー。**

インストール不要。ZIP を展開して起動するだけです。レジストリにも
`AppData` にも書き込みません。**やめるときはフォルダごと削除して終わりです。**

> ⚠ **現在ベータ版です。** 動作の確認と不具合の洗い出しを進めている段階です。
> 大切なファイルを扱う操作は、まずコピーで試すことをおすすめします。

---

## 何ができるか

- **2 画面で並べて、コピー・移動する** — コピー元と先を同時に開いておけるので、
  フォルダを行ったり来たりせずに済みます
- **タブ** — よく使うフォルダを開いたままにしておけます
- **待たされない** — フォルダの中身を裏で読み込み、一度見たフォルダは記憶します。
  OneDrive や NAS のように**応答が遅い場所でも、画面が固まりません**
- **お気に入り** — よく行くフォルダへ一発で飛べます
- **フォルダごとの色分け** — 大事なフォルダを見分けやすくできます
- **上書きされるファイルをゴミ箱へ送ってから上書き**（既定）— 上書き事故から救えます

操作は **Windows エクスプローラーとほぼ同じ**です。違うところだけ、
下の「Windows と動きが違うところ」にまとめてあります。

---

## 動作環境

| | |
|---|---|
| OS | Windows 10 / 11（64bit） |
| インストール | **不要**（ZIP を展開するだけ） |
| 追加ソフト | **不要**（[Everything](https://www.voidtools.com/) があると一部の機能が増えますが、無くても普通に使えます） |

---

<!-- zip -->
## 使い方（3 ステップ）

1. **[Releases](https://github.com/kiosq911/sak2/releases) から ZIP をダウンロードする**
2. **展開する** — ⚠ **`C:\Program Files\` 以外**に置いてください（理由は下記）
3. **`sak2.exe` を起動する**

### ⚠ 置き場所について（大事）

sak2 は設定を **exe と同じフォルダ**に保存します。

| 何を | どこへ |
|---|---|
| 設定 | `<sak2.exe のフォルダ>\config.json` |
| 履歴・表示キャッシュ | `<sak2.exe のフォルダ>\_cache\` |

このため **`C:\Program Files\` の下に置くと、設定が保存できません**
（管理者権限が要るフォルダのため）。Windows が別の場所へ勝手に書き込み先を
変えてしまい、**「設定したのに次に開くと元に戻っている」**という状態になります。

**おすすめの置き場所**: `C:\tool\sak2\` や `D:\apps\sak2\` など、
ふだん自分で読み書きできるフォルダ。

この設計の利点は、**アンインストールがフォルダの削除だけで済む**ことです。
どこにも痕跡を残しません。

---

<!-- zip -->
## 「WindowsによってPCが保護されました」と出たときは

初回の起動で、青い画面にこう表示されることがあります。

> **WindowsによってPCが保護されました**
> Microsoft Defender SmartScreen は認識されないアプリの起動を停止しました。

**これは「ウイルスを見つけた」という意味ではありません。**
「**この配布元をまだ見たことがない**」という意味です。

Windows は、有償のコード署名証明書（年額数万円）が付いていないアプリすべてに
この警告を出します。個人が無償で公開しているソフトの多くが同じ状態です。

### 起動する手順

1. 「**詳細情報**」をクリックする
2. 下に出てくる「**実行**」をクリックする

以降は表示されません。

### それでも不安なときは

**このソフトが安全であることを、自分で確かめられます。**

- **VirusTotal のスキャン結果** — 各リリースのページに、70 社以上のウイルス対策
  エンジンでスキャンした結果へのリンクを載せています
- **SHA-256 ハッシュ** — 各リリースのページに記載しています。
  ダウンロードしたファイルが途中ですり替わっていないか確認できます

  ```powershell
  Get-FileHash sak2.zip -Algorithm SHA256
  ```

  この値がリリースページの記載と一致すれば、配布したものと同一です。

---

## 通信について

**sak2 本体（`sak2.exe`）は、ネットワーク通信を一切行いません。**

| | 通信するか |
|---|---|
| `sak2.exe`（本体） | **しない** |
| `sak2config.exe`（設定画面） | 「更新を確認」を**押したときだけ** GitHub を見に行きます |
| `sak2更新.bat` | 実行したときだけ GitHub からダウンロードします |

更新の自動確認は**既定でオフ**です。オンにしたい場合は設定画面から変更できます。

> Everything と連携する機能は、**同じ PC の中でのやりとり**（ウィンドウメッセージ）
> であり、ネットワーク通信ではありません。

なお、sak2 から開いたファイルを処理する**他のアプリの動作までは関知できません**。

---

## Everything があるとできること（任意）

[Everything](https://www.voidtools.com/) が入っていると、次の機能が使えます。
**入っていなくても本体は普通に動きます**（その機能が使えないだけです）。

| 機能 | 必要なもの |
|---|---|
| 検索パネル（Ctrl+F） | Everything が起動していること |
| フォルダの合計サイズを表示 | Everything の起動 ＋ フォルダサイズのインデックス有効化（既定オフ） |
| OneDrive の状態表示を速くする | **Everything 1.5 以上**（1.4 では効きません） |

設定画面の「外部ツール連携」から切り替えられます。

> **Everything 1.4 をお使いの場合**は、「OneDrive の状態表示を速くする」を
> **オフ**にしてください。1.4 では連携が成立せず、問い合わせて失敗してから
> 自分で調べ直すため、かえって遅くなります。

---

## Windows と動きが違うところ

sak2 は **エクスプローラーの操作をそのまま使えること**を基本にしています。
下の表にあるものだけが、意図的に既定を変えている箇所です。
**多くは設定で Windows 標準へ戻せます。**

| 項目 | sak2 の既定 | Windows 標準 | 戻し方 |
|---|---|---|---|
| 上書きされるファイル | **ゴミ箱へ送ってから上書き**（後から救える） | 直接消して上書き（復元できない） | 設定 → 動作 →「上書きされるファイルをゴミ箱へ送る」をオフ |
| 隠しファイル | **表示する** | 表示しない | 設定 → 詳細設定 → `show_hidden` を false |
| システムファイル（`desktop.ini` 等） | 表示しない | 表示しない | 設定 → 詳細設定 → `show_system` を true で表示 |
| OneDrive の状態表示 | 「状態」列に記号 1 文字（`☁`＝クラウドのみ／`✓`＝PC にある） | アイコンの重ね表示 | 設定なし |
| ヘルプ（F1） | 何も起きない | ヘルプの Web 検索が開く | 設定なし（機能を搭載していません） |

表に無い挙動は、Windows 標準に合わせてあります。

---

<!-- zip -->
## 同梱ファイル

| ファイル | 役割 |
|---|---|
| `sak2.exe` | 本体 |
| `sak2config.exe` | 設定画面 |
| `sak2report.exe` | 異常終了したときの報告画面 |
| `sak2更新.bat` | 新しい版のダウンロードと入れ替え |
| `LICENSE` | 使用許諾条件 |
| `THIRD_PARTY_LICENSES.txt` | 使用しているライブラリの権利表示 |

---

<!-- zip -->
## 更新のしかた

- **設定画面から** — 「メンテナンス」→「更新を確認」
- **`sak2更新.bat` を実行する** — 最新版を取得して入れ替えます
  （ベータ版も対象にしたい場合は `sak2更新.bat BETA`）

前の版は `sak2.exe.bak` として残るので、問題があれば戻せます。

---

<!-- zip -->
## 不具合の報告・要望

[Issues](https://github.com/kiosq911/sak2/issues) へお願いします。

報告のときは、次があると助かります。

- Windows のバージョン（10 / 11）
- sak2 のバージョン（タイトルバーに出ています）
- 何をしたときに、何が起きたか

---

<!-- zip -->
## ライセンス

**無償で使えます。個人利用・業務利用を問いません。**

- ✅ 自由に使える（職場の PC でも、連絡不要）
- ✅ 内容を変えないままなら、自由に再配布できる
- ✅ 紹介・レビュー・実況は自由（広告が付いていても構いません）
- ❌ 販売・有償配布は禁止
- ❌ 逆コンパイル・改造・改造版の配布は禁止

詳しくは同梱の `LICENSE` をご覧ください。

**ソースコードは公開していません。** その代わり、安全性は
[VirusTotal のスキャン結果](#それでも不安なときは)と SHA-256 で確かめられます。
